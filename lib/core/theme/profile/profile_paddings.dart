import '../../../core_import.dart';

class ProfilePaddings {
  ProfilePaddings._();

  static const EdgeInsets page = EdgeInsets.symmetric(horizontal: 24);
  static const EdgeInsets pageWithTop = EdgeInsets.fromLTRB(24, 24, 24, 0);
  static const EdgeInsets buttonPadding = EdgeInsets.symmetric(vertical: 17);
  static const EdgeInsets noteCard = EdgeInsets.all(14);
  static const EdgeInsets chipPadding = EdgeInsets.symmetric(
    horizontal: 14,
    vertical: 8,
  );
}
