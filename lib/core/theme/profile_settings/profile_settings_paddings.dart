import 'package:flutter/material.dart';

class ProfileSettingsPaddings {
  ProfileSettingsPaddings._();

  static const EdgeInsets page = EdgeInsets.symmetric(horizontal: 20);
  static const EdgeInsets cardInner = EdgeInsets.all(16);
  static const EdgeInsets section = EdgeInsets.symmetric(horizontal: 20);
}
